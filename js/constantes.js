 export const PI = Math.PI; // con esto hago que se exporten las lineas de codigo

  // solo se permite exportar por defecto solo una  funcion o atributo
 export default function miSaludo(){
     console.log('el saludo de facundo');
 }